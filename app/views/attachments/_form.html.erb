<%= render_header %>

<%= render_form @attachment, {
        fields: {
            assets: 'file_field'
        },
        html: {
                multipart: true
        }

} %>

<%= show_link @attachment %> |
<%= back_link %>
