<%= render_header %>

<table>
  <tr>
    <th>User</th>
    <th>Attachment</th>
    <th></th>
    <th></th>
  </tr>

<% @attachments.each do |attachment| %>
  <tr>
    <td><%= attachment.user_id %></td>
    <td><%= image_tag attachment.assets.url(:thumb) %></td>
    <%= action_links attachment, edit_attachment_path(attachment) %>
  </tr>
<% end %>
</table>

<br />

<%= new_link new_attachment_path %>