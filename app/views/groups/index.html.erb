<h1>Listing groups</h1>

<table>
  <tr>
    <th>User</th>
    <th>Name</th>
    <th>Description</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @groups.each do |group| %>
  <tr>
    <td><%= group.user_id %></td>
    <td><%= group.name %></td>
    <td><%= group.description %></td>
    <%= action_list group, edit_group_path(group) %>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Group', new_group_path %>
